- name: Install apt packages
  ansible.builtin.apt:
    name: 
    - git
    - qemu-guest-agent
    - openssh-server
    - neovim
    - chromium
    - deja-dup
    state: latest

- name: Install flatpak packages
  flatpak:
    name: "{{ item }}"
    state: present
  with_items:
    - com.brave.Browser
    - org.freecad.FreeCAD

